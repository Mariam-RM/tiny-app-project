<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>

  <% include partials/_header %>

  <h2> page to show single url and its associated shortform </h2>
    <%= shortKey %> : <%= urls[shortKey].longURL %>
    <form method="POST" action="/urls/<%= shortKey %> ">
      <input value="<%= urls[shortKey].longURL %>" type="text" name="longURL" >
      <input type="submit" value="update">
    </form>

</body>
</html>